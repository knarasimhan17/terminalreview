# trv

This repository is public. Never commit secrets, tokens, credentials, private keys, `.env` files, or personal information.

## Commits

Small conventional commits (`feat:`, `fix:`, `test:`, `docs:`, `refactor:`, `chore:`). One completed change per commit.

## Tests

Run `cargo test` before shipping. Every bug fix includes a regression test that fails without the fix.
